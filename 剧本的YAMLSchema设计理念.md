# 剧本文档 YAML Schema 设计说明

## 1. 文档目的

本文档用于定义剧本文档的 YAML Schema，用于描述一个可被系统解析、校验和处理的剧本数据结构。该 Schema 与 Java 根对象 `ScriptDocument` 一一对应，所有字段均严格遵循 Jakarta Bean Validation 的校验规则，以保证文档内容在进入业务系统前具备完整性、一致性和可扩展性。

本 Schema 主要服务于以下场景：

- 剧本生成结果的结构化存储
- 剧本编辑器与后端之间的数据交换
- 自动化校验与入库
- 后续的版本兼容与能力扩展

---

## 2. 设计目标

该 Schema 的设计遵循以下几个目标：

### 2.1 结构清晰

剧本文档天然包含元信息、改编目标、人物、地点、场景和场景内容等多个层次，因此 Schema 采用分层结构，便于阅读、维护和程序解析。

### 2.2 便于校验

通过与 Jakarta Bean Validation 对齐，能够在 Java 后端直接对 YAML 反序列化后的对象进行字段校验，减少额外的手工校验逻辑，提高系统稳定性。

### 2.3 便于引用

人物、地点、场景等对象使用 `id` 进行关联，而不是在场景中重复写完整信息。这种方式可以避免数据冗余，并保证引用关系的一致性。

### 2.4 兼顾扩展性

Schema 中保留了版本号、可选字段和修订备注等设计，使文档既能满足当前需求，也能支持未来增加新的字段、内容块类型或处理流程。

### 2.5 适合生成式场景

剧本往往来自 AI 生成、人工修订或多轮加工。Schema 需要既能表达完整剧本，也能容纳部分可选信息，因此必填与可选字段做了明确区分。

---

## 3. 根对象定义

剧本文档的根对象为 `ScriptDocument`，包含以下核心部分：

- `schemaVersion`：文档结构版本
- `metadata`：剧本元信息
- `adaptation`：改编意图
- `characters`：人物档案列表
- `locations`：地点档案列表
- `scenes`：场景列表
- `revisionNotes`：修订备注

这种划分方式的设计原因是：将“文档属性”“创作意图”“资源对象”“叙事内容”分离，既符合剧本编辑的实际组织方式，也便于系统按模块处理数据。

---

## 4. YAML Schema 定义

```yaml
# 剧本文档根对象 -> 对应 Java: ScriptDocument
# 所有字段严格遵循 Jakarta Bean Validation 校验规则

# 文档结构版本 非空必填
schemaVersion: "1.0"

# 剧本元信息 非空 + 嵌套校验 对应 Java: ScriptMetadata
metadata:
  title: ""                # 作品标题 非空必填
  language: "zh-CN"        # 文档语言 非空必填
  sourceChapterCount: 3    # 原文章节数量 最小值 3 可改为 ≥3 的任意数字
  generatedAt: ""          # 生成时间 非空必填 格式：2026-01-01T00:00:00Z

# 改编意图 非空 + 嵌套校验 对应 Java: Adaptation
adaptation:
  format: ""               # 剧本形式 如 web_drama 非空必填
  tone: ""                 # 整体风格 如 suspense 非空必填
  logline: ""              # 故事一句话梗概 非空必填
  themes: []               # 主题关键词 可选 可为空数组

# 人物档案列表 非空集合 至少1个 对应 Java: CharacterProfile
characters:
  - id: "char_001"         # 人物唯一编号 非空必填
    name: ""               # 人物姓名 非空必填
    aliases: []            # 人物别名 可选
    role: ""               # 角色定位 如 protagonist 非空必填
    goal: null             # 角色目标 可选
    traits: []             # 性格特征 可选
    voice: null            # 语言风格 可选

# 地点档案列表 非空集合 至少1个 对应 Java: LocationProfile
locations:
  - id: "loc_001"          # 地点唯一编号 非空必填
    name: ""               # 地点名称 非空必填
    type: ""               # 地点类型 如 interior/exterior 非空必填
    description: ""        # 地点描述 可选

# 场景列表 非空集合 至少1个 对应 Java: Scene
scenes:
  - id: "scene_001"        # 场景唯一编号 非空必填
    title: ""              # 场景标题 非空必填
    sourceChapters: []     # 对应原文章节编号 非空集合 至少填1个
    locationId: ""         # 引用地点ID 对应 locations.id 非空必填
    timeOfDay: ""          # 场景时间 如 night/day 非空必填
    characters: []         # 出场人物ID列表 非空集合 至少1个
    dramaticPurpose: ""    # 戏剧目的 非空必填
    summary: ""            # 场景摘要 非空必填
    # 场景内容块 非空集合 至少1个 对应 Java: SceneBlock
    blocks:
      - type: ""           # 内容块类型 action/dialogue/narration 非空必填
        speakerId: null    # 说话人ID 仅对白填 其他可为null
        text: ""           # 内容文本 非空必填

# 修订备注 可选 可为null或空数组
revisionNotes: []
```

---

## 5. 字段设计说明

## 5.1 `schemaVersion`

`schemaVersion` 是文档级必填字段，用于标识当前 YAML 所遵循的结构版本。

设计原因：

- 便于后续升级 Schema 时进行兼容处理
- 允许系统根据版本选择不同的解析或迁移逻辑
- 避免旧文档在新系统中因字段变化而产生歧义

如果未来增加新的字段、调整枚举值或修改约束，可以通过提升版本号来区分。

---

## 5.2 `metadata`

`metadata` 用于描述剧本文档本身的基础信息，包括标题、语言、原文章节数量和生成时间。

设计原因：

- `title` 用于识别作品，是最基本的业务标识
- `language` 明确文档语言，有利于多语言处理、分词和文本生成策略切换
- `sourceChapterCount` 表示原始素材规模，为改编范围和完整性校验提供依据
- `generatedAt` 记录生成时间，便于追踪生成批次、版本和审计信息

其中 `sourceChapterCount` 设置最小值为 `3`，意味着该 Schema 面向“多章节改编”的剧本场景，而不是单句、单段或极短文本任务。

---

## 5.3 `adaptation`

`adaptation` 用于表达改编方向，而不仅仅是记录结果。

设计原因：

- `format` 指定剧本形式，如短剧、舞台剧等，有助于后续控制节奏、结构和输出样式
- `tone` 描述整体风格，如悬疑、轻喜、古风等，便于内容生成和审核
- `logline` 用一句话概括故事核心，帮助系统和人工快速理解作品定位
- `themes` 作为主题关键词集合，为检索、推荐、标签化和二次创作提供支持

将改编意图独立出来，而不是混在元信息中，是因为它描述的是“创作目标”，不是“文档属性”。

---

## 5.4 `characters`

`characters` 为人物档案列表，每个角色以独立对象表示。

设计原因：

- 将人物单独建模，可以避免在多个场景中重复描述人物信息
- 通过 `id` 建立稳定引用关系，适合复杂剧情中的多场景复用
- 角色的 `goal`、`traits`、`voice` 等信息有助于后续生成对白和行为一致性

字段设计说明：

- `id`：用于唯一标识角色，避免重名问题
- `name`：角色对外显示名称
- `aliases`：支持别名、称呼变化或身份伪装
- `role`：用于标记主角、反派、配角等叙事定位
- `goal`：可选，因为有些角色在初稿阶段尚未明确目标
- `traits`：可选，用于补充人物性格维度
- `voice`：可选，用于约束人物语言风格

`characters` 至少要求 1 个元素，是因为无人物的剧本场景通常不构成有效叙事对象。

---

## 5.5 `locations`

`locations` 为地点档案列表。

设计原因：

- 地点是场景组织的重要依据，将其独立建模可以复用
- 使用 `locationId` 引用地点，避免同一地点描述在多个场景中重复出现
- 独立地点档案也便于后续扩展视觉风格、布景说明、拍摄条件等信息

字段设计说明：

- `id`：地点唯一标识
- `name`：地点名称
- `type`：地点类型，如室内、室外等
- `description`：补充地点氛围和环境特征

`description` 允许为空，是因为某些流程中地点先被引用，详细描述可以后补。

---

## 5.6 `scenes`

`scenes` 是整个 Schema 的核心，用于承载实际叙事内容。

设计原因：

- 剧本天然以场景为基本组织单位
- 场景能够关联原文章节、地点、出场人物和戏剧目的
- 分场景存储有利于剪辑、审核、重写、重排和多端展示

字段设计说明：

- `id`：场景唯一编号，便于检索、修改和引用
- `title`：场景标题，帮助快速定位内容
- `sourceChapters`：记录场景来源章节，支持溯源
- `locationId`：关联地点对象，确保结构统一
- `timeOfDay`：表达时间维度，如白天、夜晚、黄昏
- `characters`：列出出场人物，保证场景涉及的角色是明确的
- `dramaticPurpose`：明确该场景在叙事中的功能，如铺垫、冲突、反转、揭示信息
- `summary`：提供场景摘要，方便快速浏览和检索
- `blocks`：承载具体的场景内容块

`sourceChapters` 至少 1 个，是因为每个场景都应能够追溯到原始素材来源；这样可以支持改编质量检查和章节覆盖分析。

---

## 5.7 `blocks`

`blocks` 表示场景内部的内容块，是对剧本正文的进一步结构化拆分。

设计原因：

- 将场景正文拆为 `action`、`dialogue`、`narration` 等块，便于程序识别内容类型
- 可以支持更细粒度的渲染、编辑、审核和后续生成
- 有助于对白抽取、角色语气分析、镜头脚本转换等功能

字段设计说明：

- `type`：表示内容块类型，建议值包括 `action`、`dialogue`、`narration`
- `speakerId`：仅在对白块中需要；动作和旁白可为 `null`
- `text`：块的正文内容，必须非空

`speakerId` 允许为 `null` 的设计原因在于，不是所有文本块都属于某个角色发言。例如动作描写和旁白本身就不应绑定角色。

---

## 5.8 `revisionNotes`

`revisionNotes` 用于记录修订说明，可为空数组或 `null`。

设计原因：

- 剧本往往存在多轮修改，保留修订备注有助于追踪变化
- 作为可选字段，不会增加最小可用文档的编写负担
- 为后续人工协作、审核流和版本对比提供信息基础

之所以不设为必填，是因为初版生成结果不一定立即带有修订历史。

---

## 6. 校验规则设计原因

本 Schema 明确区分了必填字段、可选字段、非空集合和最小值约束，原因如下：

### 6.1 必填字段保证最小可用性

例如 `title`、`format`、`tone`、`summary`、`text` 等字段若为空，剧本文档将失去基本业务意义，因此必须强制填写。

### 6.2 集合最小数量保证结构完整

例如：

- `characters` 至少 1 个
- `locations` 至少 1 个
- `scenes` 至少 1 个
- `blocks` 至少 1 个
- `sourceChapters` 至少 1 个

这些约束确保文档不是空壳，而是真正可消费、可处理的剧本数据。

### 6.3 可选字段支持渐进完善

例如 `goal`、`voice`、`description`、`revisionNotes` 等字段允许为空，是为了适应“先生成骨架，再逐步补全细节”的实际工作流。

### 6.4 嵌套校验保证全链路一致性

`metadata`、`adaptation`、`scenes`、`blocks` 等对象采用嵌套校验，能够确保不仅根对象存在，而且内部字段也满足要求，避免出现“对象不为空但内容无效”的情况。

---

## 7. 引用式建模的设计原因

该 Schema 没有在场景中直接嵌入完整人物或地点对象，而是通过 `characterId` 和 `locationId` 进行引用。这种设计具有以下优点：

- 避免重复数据，提高文档一致性
- 一个地点或人物可以在多个场景中复用
- 修改人物或地点信息时只需要改一处
- 更适合数据库存储、索引和跨模块处理

这种引用式建模方式尤其适合中长篇剧本，因为人物和地点在多个场景中反复出现，重复嵌套会导致维护成本明显上升。

---

## 8. 扩展性设计说明

Schema 通过以下方式保留扩展空间：

- 使用 `schemaVersion` 支持版本演进
- `themes`、`aliases`、`traits` 等列表字段便于增加标签类信息
- `blocks.type` 可在未来扩展更多内容块类型，如 `transition`、`camera_note`、`sfx`
- `revisionNotes` 为后续人工编辑与审校流程预留入口

因此，该 Schema 既适用于当前剧本生成场景，也适合作为后续创作平台的数据基础模型。

---

## 9. 总结

该剧本文档 YAML Schema 的核心价值在于：以清晰的层次结构表达剧本内容，以严格的校验规则保证数据质量，以引用式建模降低冗余，并通过版本号和可选字段预留扩展能力。

- 从工程角度看，这一设计便于 Java 后端解析与校验；
- 从业务角度看，它能够支持剧本生成、编辑、检索、审校和版本演进等多种场景；
- 从创作流程看，它也兼容“先生成框架、后补充细节”的实际使用方式。

谢谢。